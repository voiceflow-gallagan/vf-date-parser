{
  "name": "vf-date-parser",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "docker-build": "docker-compose down --rmi all && docker-compose up --remove-orphans --build -d",
    "docker-start": "docker-compose down && docker-compose up --remove-orphans --build -d",
    "docker-debug": "docker-compose down && docker-compose up --remove-orphans --build",
    "docker-stop": "docker-compose down"
  },
  "author": "Voiceflow",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.3.2",
    "express": "^4.18.2",
    "parse-human-date": "^1.2.0"
  }
}
